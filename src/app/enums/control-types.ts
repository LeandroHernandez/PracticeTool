export enum ControlTypes {
  controlArray = 'controlArray',
  number = 'number',
  text = 'text',
  email = 'email',
  password = 'password',
  textarea = 'textarea',
  select = 'select',
  multiselect = 'multiselect',
  group = 'group',
}
